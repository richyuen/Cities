# Integrator todo (applied between waves, when no builders are running)

- [x] materials.js: don't pass `normalScale: undefined` (audio request) — done
- [x] renderer.js: PCFSoftShadowMap removed in r186 → PCFShadowMap — done
- [x] assets.js: RGBELoader → HDRLoader — done
- [x] context.js: filter D3D shader-compiler X4122 noise from captured warnings — done
- [x] world.js: removeRoad unmarks cells before deleting nodes (ui request) — done
- [x] ARCHITECTURE §4: sim:tick payload gains `demand:{r,c,i}` (ui request)
- [x] FrameStats: add GPU frame time via `EXT_disjoint_timer_query_webgl2` (`gpuMs`), expose in `__city.stats()` and shot.mjs JSON. Reason: headless fps is vsync-pinned at 144 so fps alone can't prove the 50 fps budget (effects builder measured with timer queries).
- [x] ARCHITECTURE §7: effects draw-call budget → ≤ 25 (fixed post overhead: GTAO 2, grade 1, bloom 13, output 1, SMAA 3).
- [x] ARCHITECTURE §7: note that UnrealBloom thresholds on luminance — red/blue emissives need `emissiveIntensity ≥ 4` to bloom at night; yellow/white 2.5–3 is fine.
- [x] BUILDER_BRIEF: fps in JSON is vsync-pinned; use `gpuMs`/`frameMs` once available.
- [x] main.js: every `?showcase=<id>` also loads `environment` (if present and loaded) so showcases get real sun/sky/night; keep fallback only when environment is missing/failed. (audio critic: night looks like noon in all showcases)
- [x] fallbackLights.js: dim env-map intensity + hemi at night (materials.setEnvIntensity), sky colour via sRGB.
- [x] Rebuild queue after integrator pass: audio r2 (5.5), simulation r2 (5.5) — both blocked on showcase lighting
- [x] DECISION (effects critic #3): the effects/composer path is canonical for the shipped game. Sky tone-mapping: environment must look right under the composer (OutputPass tone-maps everything incl. Sky). Direct render is fallback only; effects may drop its skyBypass. Document in ARCHITECTURE §7.
- [x] Rebuild queue: effects r2 (6.5) after integrator pass (needs environment in showcase closure).
- [ ] critic queue (after environment r2 lands): audio r2, simulation r2, effects r2, ui r3
- [ ] TERRAIN: capped at round 4/4, FAIL at 7.3. Root issue: terraces cast no legible shadow at normal play/overview distance (technical shadow pipeline verified working via receiveShadow-toggle test, but magnitude too subtle vs strong ambient fill; baked vertex-color shading was tried instead of real shadow tuning and didn't move the score). If resumed later, the lever is likely environment's shadow bias/radius/softness for small-scale geometry, or a deliberate large increase in terrace step height in terrain/heightgen.js's stepFor(), verified with a full critic pass. Not blocking other modules; flag for the whole-game critic and final report.
- [ ] ENVIRONMENT: capped at round 4/4, FAIL at 8.0 (close). Real progress each round (5.5→7.5→7.7→8.0). Remaining issue is confined to environment's own showcase test-rig terrain (basin/amphitheresis effect from every azimuth, softened-not-solved flat near-zone) — does NOT affect the shipped game (terrain module owns real ground). Core lighting/sky/shadow/weather/moon/reflections all strong and stable since r2. Not blocking; flag for whole-game critic and final report.
- [ ] WAVE 1 COMPLETE: 5/7 pass (ui 8.9, simulation 8.8, effects 8.6, roads 8.6, audio 8.6). 2/7 capped-fail (environment 8.0, terrain 7.3). Proceeding to Wave 2 (zoning, buildings, props, traffic, tools) per plan.
- [ ] world.js: setZone() can flip a type:'building' cell back to type:'zone' if called directly (bypasses zoning module's own guard). Any future caller (tools, simulation) must go through zoning's api or world.js should itself refuse to zone a cell with a buildingId. (found by zoning builder)
- [ ] tools/critic.mjs: first shot of a batch can come back as a cold-start/blank frame (page-navigation race) — add an extra settle-frame before the first capture in a batch. (found by zoning r2 critic, self-corrected in that run)
- [x] camera.js: added clampOutsideBuildings() — per-frame AABB push-out of the camera eye from any building footprint below roofline (docs/core-requests/buildings.md). Cheap linear scan over world.buildings. Verified: loads clean, zero errors.
- [ ] WAVE 2 COMPLETE: 5/5 pass (buildings 8.7, props 8.7, tools 8.7, zoning 8.6, traffic 8.6). Proceeding to Wave 3 (demo city).
- [x] DECISION: relaxed the full-city triangle budget (was 3M, unrealistic for Lego stud/bevel geometry — terrain+CSM alone is 1.8M). gpuMs is now the authoritative perf metric per ARCHITECTURE §0/§7. Demo city measured 14.7-20M triangles at 73+fps / <=12.8ms GPU / 700-780 draw calls on the reference RTX 4070 Ti — both real budgets (fps, draw calls, gpuMs) pass with margin. Documented in ARCHITECTURE.md §0.
- [ ] WAVE 3 (demo city) built: 300 buildings, 140 road edges, 4 parks/276 park cells, 2051 props, ~165 cars, zero console errors across 16+ screenshots. Ready for whole-game critic.
- [ ] WHOLE-GAME CRITIC round 1: score 4.5, FAIL. Critical issue #1 (pale/washed-out overview+aerial after prior camera change, likely CSM matrixWorld staleness) dispatched to a dedicated fix agent. Minor issues queued for after the critical fix: (2) rain weather has no streak particles/wet-road specular; (3) traffic reads sparse from wide/elevated framing; (4) demand HUD bars read near-empty on a balanced day-1 city (likely correct, sanity check only).
- [x] effects: fixed a real sceneRT/pingRT aliasing bug (OutputPass/SMAAPass writing into the live MSAA depth target) — kept, verified no regression, but NOT the cause of the toothpick bug (proven: bug persists with effects.setEnabled(false) entirely).
- [ ] WHOLE-GAME critical bug round 3: root-cause investigation now targets environment's PMREM regenEnv()/scene.environment as the likely culprit (envMap+clearcoat-only symptom, requires-sustained-frames trigger pattern matches periodic regen). Dispatched with a decisive freeze-env test. environment's ineffective kickEffectsRenderTargets() workaround to be removed once real fix lands.
- [x] ROOT CAUSE CONFIRMED AND FIXED (round 3): environment's regenEnv() was allocating a brand-new PMREMGenerator render target/texture on every regeneration (PMREMGenerator.fromScene() has no reuse option), churning scene.environment's texture identity across a session. That identity churn corrupted MeshPhysicalMaterial envMap sampling on distant/steep views. Fixed by reimplementing fromScene()'s body to render into a persistent, reused target. Verified with heavy churn testing (120 rapid preset/tod bounces) - clean. Ineffective kickEffectsRenderTargets() workaround removed.
- [ ] WHOLE-GAME round 3: score 4.5, FAIL. Camera/tod-history trigger genuinely fixed (verified under 2400+ frame heavy churn). NEW cleanly-isolated trigger remains: weather (rain/fog) + steep camera pitch (overview/aerial) reproduces the toothpick bug on a completely fresh load, no history needed. Also a new CPU-side perf regression (43fps at street-6h5/closeup-21h5, below 50fps floor) appeared alongside the round-3 fix. Round 4 investigation dispatched targeting shadowRadius/weather interaction with CSM at steep pitch, plus the perf regression.
- [x] ROOT CAUSE CONFIRMED AND FIXED (round 4): environment's FogExp2 density formula was tuned only for street-scale distances; being quadratic-in-distance it fully saturates by ~800-1000m, washing overview/aerial (~1246m) to flat grey under rain/fog. Fixed with a per-frame density cap keyed to live camera-target distance (FOG_CAP_K=0.9). Verified: 20/20 game batch clean, dedicated overview/aerial x tod x rain/fog sweep (16/16) clean, skyline/street controls unaffected.
- [ ] PERF REGRESSION root-caused (not environment's): buildings' autoGrowthPass()/S.batcher.flush() periodic mesh rebuild stalls 500-600ms every ~2.5s, dropping street-6h5/closeup-21h5 below the 50fps floor. Dispatched to buildings for a fix.
- [x] PERF REGRESSION FIXED: buildings had two stacked causes (chunk-granularity batching rebuilding whole quadrants, and unbounded synchronous level-up processing of 100+ candidates per poll). Both time-sliced (2.5ms/2ms per-frame budgets). Max frame cost 638ms -> under 16ms. Game batch minFps 41-50 -> 91.2. Showcases unaffected (still synchronous/immediate).
- [x] WHOLE-GAME PASSES at 8.8/10 (round 4). Bug family genuinely closed after 4 rounds: effects render-target aliasing (fixed), environment PMREM texture-identity churn (fixed), environment fog-density distance-saturation under weather at steep pitch (fixed), buildings auto-growth perf stall (fixed). Verified under ~85 adversarial shots including heavy churn with interleaved weather. minFps 92.2, maxGpuMs 9.4, maxDrawCalls 792, errors 0. Remaining minor issues: traffic reads sparse from wide angles (not blocking), demand bars near-empty on Day 1 (correct, not a bug).
